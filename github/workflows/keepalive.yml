name: Keep Render Awake

on:
  schedule:
    - cron: '*/12 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping mission control
        run: curl -sf https://emperor-os.onrender.com/health || true
