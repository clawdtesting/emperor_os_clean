// Legacy shim: canonical implementation lives in agent/validate.js.
// TODO(deprecation): remove core shim after downstream migration completes.
export { validate, validateOutput, isSubstantive } from "../agent/validate.js";
